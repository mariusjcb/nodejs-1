var ServerApplication = class {
    constructor() {
        var osInfos = Import.Node("os");
        //http.createServer(function(){console.log("incarca...")}).listen(3000);

        new ConsoleLog("server app");
    }
}